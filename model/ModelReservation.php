<?php

require_once File::build_path(array('model', 'Model.php'));

class ModelReservation{
    
    
}
Reservation (#NumResa, dateResa, commentaire, prixespacenego???, statut(annulé ou non), etatFacture(éditée,
payée)